import React from 'react'
import ReactDOM from 'react-dom/client'
import ComposantFormulaire from './jsx/ComposantFormulaire';

ReactDOM.createRoot(document.getElementById('formulaire-app')).render(
    <React.StrictMode>
        <ComposantFormulaire />
    </React.StrictMode>,
)