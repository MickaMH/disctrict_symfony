{% extends 'base.html.twig' %}

{% block title %}Lien de réinitialisation{% endblock %}

{% block body %}

<main>

    <div class="container mb-5">

        <div class="row">

            <div class="col col-md-8 col-lg-8 mx-auto">

                <p class="fs-1 mb-5 text-center">Nouveau mot de passe</p >

                {# <p>
                    If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
                    This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p> #}
                <p>
                    Si un compte correspond à cette adresse email, un email contenant un lien de réinitialisation de votre mot de passe vous a été envoyé.
                    Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                    Vous pouvez fermer cette page.
                </p>

                {# <p>If you don't receive an email please check your spam folder or <a href="{{ path('app_forgot_password_request') }}">try again</a>.</p> #}
                <p>Si vous n'avez pas reçu d'email, consultez le dossier spam ou essayez à nouveau <a href="{{ path('app_forgot_password_request') }}">Réinitialisation</a></p>

            </div>

        </div>

    </div>

</main>

{% endblock %}
